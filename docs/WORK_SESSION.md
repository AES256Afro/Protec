# Platform build until complete

- Authorization: continue until the requested platform roadmap is complete. User removed the time cutoff on 2026-09-08 at 16:43 UTC.
- Project: `/Users/chris/Projects/Protec`; remote: `AES256Afro/Protec`; branch: `main`.
- Continuation automation: `protec-four-hour-platform-build`, every 10 minutes without a time cutoff. Do not duplicate it.
- Local device: MacBook, already enrolled. Existing foreground control plane listens on 127.0.0.1:8765 and agent uses `.protec/agent.json`.
- Source of milestone scope/dependencies: `docs/ROADMAP.md`.
- Baseline: `9e3019a`, 9 Python tests and 2 JavaScript tests passed, hosted Linux CI passed.

The earlier four-hour schedules are superseded. Continue independent work until milestone acceptance criteria are satisfied. If actual hardware, credentials, or a deployment decision is required, complete independent slices before asking a concrete question.

## Current work

- M0/M1: local inventory, enrollment lifecycle and MacBook onboarding delivered.
- M2: backup/staged restore, transactional migrations, paginated history, readiness and conservative retention delivered. Audit retention is intentionally preserved; production resilience remains M15.
- M6 preparation: read-only Homebrew formula and installed Debian package inventory delivered at d29ccac with hosted CI passing. Last local package report contained 34 formulae. Package mutation awaits M3/M4.
- M3 first slice: local service credentials with viewer/operator/administrator roles, 1-720 hour expiry, revocation, metadata pagination and actor attribution implemented. UI hides actions outside the role; server independently rejects unauthorized routes. Bootstrap token remains intact. SSO/MFA, device-specific scopes, rotation recovery and protected secret stores remain.

## Latest verification

- 34 Python tests plus 2 JavaScript tests passed locally. Tests exercise role escalation denials, device/service identity separation, expiry/revocation, safe metadata and pagination.
- Browser verified viewer restrictions, administrator Access view, test credential issuance and clearing its one-time secret on close. Credential tests used a disposable database, not the live workspace.
- Pre-schema-v2.db backup created. Copy of the live schema-1 database upgraded to schema 2 with device records intact and no issued service credentials. Live control plane restarted on schema 2; bootstrap administrator and device check-ins verified. No live service credentials issued. Verify latest exact-commit CI at the next checkpoint.
- Current foreground control plane and agent use .protec; inspect process ids before restarting. The agent reports packages every five minutes or upon an inventory refresh.

## Next work

1. Live schema-2 verification completed; publish this checkpoint and verify hosted CI.
2. Continue M3: device-specific authorization scopes and recoverable credential rotation. Keep role/target checks centralized; avoid parallel implementations per feature.
3. Continue M4 typed, signed, bounded execution. M5 policy/configuration previews and read-only network/SSH/WireGuard inventory can proceed while external identity/native dependencies remain.

There is no deadline. Continue all meaningful independent milestones. Only pause for completion, user cancellation, or a concrete external dependency after independent work is exhausted. Do not mark the full M3 or platform complete based on this local credential foundation.

## Hosting milestone in progress, September 8

User explicitly authorized deployment on BigBox, an independent deployment path, an optional BoxPilot catalog entry, and a public website/demo on foragefournuts.com. The demo must track shipped milestones with mock data and matching screens. These requirements take priority over the older next-work list.

Implemented: Gunicorn WSGI adapter with shared route authorization, explicit HTTPS origin checking, readiness endpoint, non-root persistent container and Compose setup, catalog manifest in the BoxPilot checkout, and static public demo exported from the real dashboard. Tests currently pass locally: 40 Python, 4 JavaScript, BoxPilot full check with 1,628 tests. Publication and host verification are tracked below when complete.

BigBox Docker access works through the bigbox SSH account. Adding a Tailscale Serve mapping requires sudo; unattended sudo is unavailable. The existing antifascist.work tab is a fictional BoxPilot demo, not an authenticated live session. Do not use it as evidence of host access. The private live BoxPilot portal requires application login.

## Published hosting checkpoint

- Protec v0.2.0: source/image revision `07261023f45d956466074538f866e661964b374b`. Main and tag pushed. Checks 34258018439/34258022414 and image workflow 34258021933 succeeded. Public GHCR image supports linux/amd64 and linux/arm64; anonymous manifest access verified. Manifest digest `sha256:ee66f8ccc3a137eac5ab4b22907c5e0acec9497e38c04383922c6244ee987c03`.
- BigBox persistent independent Compose container is healthy. Administrator identity and a consistent database snapshot were copied securely from the local prototype. Original local server and Mac agent remain active pending the HTTPS cutover; do not claim that device check-ins have moved. There can be new writes to the local database, so repeat the backup transfer after stopping the local server and agent before cutover. Do not overwrite a remote database if new management writes have occurred there.
- Website and mock demo deployed to foragefournuts.com through Cloudflare, deployment `2211461c-e756-432e-abdd-f9033c761101`. Both public source revision and served dashboard hash match the running BigBox image. `/demo/` returned 200 with network-blocking CSP; `/api/dashboard` returned 404. Public DNS records are present; the Mac OS resolver retained a negative result during checks, while explicit public-DNS resolution verified the domain and BigBox curl resolved it normally. Worker fallback: https://protec-website.chris-c39.workers.dev.
- BoxPilot manifest committed to main as `35b3201e0dc120bea44aa9bf997eb2adc45c62dc`. Full local check: 1,628 tests. Hosted CI 34258085029 and install smoke 34258085112 succeeded. This is a catalog-source publication, not a new BoxPilot release or a verified live catalog install.
- User was asked to run the exact reviewed sudo command to install the staged catalog file and add the private Tailscale HTTPS listener. Check for their reply and inspect actual state before continuing. The running portal is independent of BoxPilot's managed-app inventory; catalog availability does not mean this existing container was installed by BoxPilot.
- Agent HTTPS fallback fixed after the image release: macOS Python without bundled CAs now uses the system CA bundle unless an explicit trust path was configured. 44 Python tests pass, and a certificate-verified HTTPS connection to BigBox succeeded. The control-plane image remains v0.2.0; this client fix does not change its dashboard.
- Existing heartbeat automation updated to require BigBox deployment, optional BoxPilot/independent installation, and public demo parity after shipped milestones. It remains active with no cutoff.

Next: complete the administrator-dependent HTTPS/catalog step, transfer the final local state without losing either side's writes, point the Mac agent to the private BigBox HTTPS origin and verify check-in/refresh, then continue M3/M4. Never call the complete infrastructure roadmap finished based on this hosting milestone.

## Device scope milestone, September 8

Implemented M3 device scopes for viewer/operator credentials: select 1-100 active device IDs at issuance, enforce targets on inventory refresh, filter dashboard counts and job/device history before paging, and deny fleet-level health/administration to scoped credentials. Administrator credentials retain fleet scope. Invalid stored scopes fail authentication. Schema 3 preserves existing schema-2 fleet credentials. The demo credential form and mock validation mirror the shipped form.

Local validation: 49 Python tests, 5 JavaScript tests, exported demo DOM scoped-issuance checks, and a migration of a consistent live database copy preserving bootstrap identity and its device record. The original Mac database and running agent remain on the prior local service until the pending HTTPS cutover. Version 0.3.0 image/site/catalog publication and BigBox upgrade are the next release checks for this checkpoint. Do not treat this as completion of all M3 identity work: SSO/MFA, protected secret storage and recoverable rotation remain.

### Verified 0.3.0 deployment

- Released source/image revision `a28698122c1ee2e1ee50157ad0524f135377911e`. Main and v0.3.0 tag pushed; hosted Checks 34260661963 and 34260664293 succeeded. Image workflow 34260664259 succeeded; multi-platform image digest `sha256:bce40718a7ffa955197d05791fa87e53cd3d70c7db7a6271e2c8f58cad2fa316`.
- Tested the published Gunicorn image in a disposable BigBox container with two fictional devices: scoped target denial, authorized refresh, filtered history/counts, fleet-health denial, and schema 3 passed. The temporary container and its data volume were removed afterward.
- Backed up the persistent portal database inside its volume to `/data/pre-scope-20260908.db` before upgrading. Preserved the previous Compose configuration on the host. Upgraded the independent persistent portal to 0.3.0; healthy and database integrity/schema 3 verified. Logical SHA-256 checks across all original table columns and bootstrap file matched before and after upgrade: 1 device, 1 job, 4 audit events, 1 enrollment, 0 service credentials. No real scoped credentials or additional devices were issued during testing.
- Published the matching public site/demo to foragefournuts.com. Cloudflare deployment `a4bed36c-be75-4bf9-b74b-38cb8c44d790`. Release metadata and actually served dashboard JavaScript match image source `a286981...`; the actual running container's dashboard checksum was compared too. Normal domain resolution now works from the Mac.
- BoxPilot catalog source updated to 0.3.0 on main at `d8bceb31546ae40e28c159dfdcce2ffe09cb5bdb`. Full local check: 1,628 tests. Hosted install smoke 34260723695 passed; verify the corresponding CI run 34260723691 before final publication report. The staged host catalog file was refreshed for the still-pending administrator step.
- The private HTTPS listener and live BoxPilot catalog install remain pending the previously requested sudo command. Do not repeatedly ask the same question. Local Mac server/agent still use the original schema-2 state. Before final cutover, compare both databases for new writes, stop local writers, take a fresh backup, and migrate safely without overwriting new BigBox records. The new BigBox image will migrate schema-2 input to schema 3.

Next independent M3 work: recoverable credential rotation with bounded overlap, explicit invalidation, and audit evidence. SSO/MFA and native protected secret stores remain. Continue typed/signed job foundations afterward. Keep demo and actual deployed release aligned at each shipped checkpoint.

## BoxPilot catalog release, September 8

- Published BoxPilot **v1.116.0** at `dbdef02b7ef5966f5d2ac27c2ea7325dc21a49ea`, including Protec 0.3.0 as catalog application 166. Both main and the tag were verified remotely. Local full check passed with 1,628 tests; hosted CI 34262553702, install smoke 34262553698, and release 34262557260 all succeeded. Release: https://github.com/AES256Afro/BoxPilot/releases/tag/v1.116.0.
- The live private BoxPilot session is now authenticated as owner. It still runs 1.114.0. Refreshed the System release check, opened **Update to v1.116.0**, and filled the version confirmation. The normal approval dialog requires the owner's password, which has not been provided. The browser tab is retained for that handoff. Do not claim the live catalog has updated until the owner completes the update and the host/UI are verified.
- Rechecked the persistent Protec container: healthy, readiness returned ok, image revision still `a28698122c1ee2e1ee50157ad0524f135377911e`. The public release check passed against that exact revision and actual served dashboard bytes. Private Tailscale port 8765 is still absent. The earlier HTTPS setup/cutover dependency remains; the BoxPilot update alone does not publish the independent container's port or migrate the Mac agent.
- Deployment documentation now explicitly makes Docker Compose the default portal installation, BoxPilot optional, and native agents separate. Docs-only Protec commit `d6c1c81` passed hosted Checks 34262709529; it does not require replacing the matching 0.3.0 portal/demo release.
- The existing every-ten-minute continuation automation remains active and already requires private deployment and matching public mock features after shipped milestones. Do not create a duplicate automation or repeatedly stage additional pending update jobs.

### Owner update completed

The user confirmed completion of the prepared update. Read-only SSH verification found BoxPilot 1.116.0 installed under `/opt/boxpilot` and its `catalog/protec.yaml` present with image 0.3.0. Protec's existing independent container remains healthy. The catalog delivery dependency is complete; no further BoxPilot upgrade approval is needed for this release.

Private HTTPS remains a separate dependency: Tailscale Serve still has no port 8765 mapping, and SSH `sudo -n` requires interactive authentication. Ask only for the remaining command, `ssh -t bigbox@bigbox 'sudo tailscale serve --bg --https=8765 http://127.0.0.1:8765'`, then verify HTTPS before the previously documented safe database/agent cutover. Do not reinstall Protec from the catalog alongside the existing independent container on the same port.

## Managed deployment and Mac cutover completed, September 8

The user enabled the private HTTPS listener, then clarified that Protec must be manageable through its installed BoxPilot card. The earlier independent-container deployment is now retired. **Use the BoxPilot-managed application for future deployments and settings changes.**

- Installed through BoxPilot's normal catalog/approval workflow on temporary loopback port 8766, transferred the verified latest database while stopped, and changed its port to 8765 through BoxPilot Settings. Container: `bp-protec`. Managed directory: `/var/lib/boxpilot-managed/catalog/protec`, with persistent `data/`. Image remains 0.3.0 at `a28698122c1ee2e1ee50157ad0524f135377911e`.
- Before transfer, stopped the local Mac server and agent and compared consistent local/remote snapshots. Enrollment, jobs, audit, credentials, and device identity matched; only newer local inventory/seen fields differed. Schema 3 migration succeeded, integrity check returned ok, and existing records were preserved. Private backup snapshots are under `.protec/*boxpilot*` (ignored by Git).
- Removed the retired independent container/network with Compose down, preserving the named `protec_data` volume. Its configuration is `/home/bigbox/protec/compose.retired.yaml`. Do not restart it on port 8765 or update it as if it were live. The original local `.protec/protec.db` is also an inactive recovery copy.
- The authoritative bootstrap token is now generated and managed by BoxPilot. Obtain it through the installed Protec card's **Sign in** or **Secrets** controls. The previous local administrator-token file is not the managed portal's token. Device credentials were preserved; `.protec/agent-before-bigbox.json` is the pre-cutover state backup.
- Updated `.protec/agent.json` to `https://bigbox.tail6233fa.ts.net:8765` and verified certificate-checked HTTPS, successful check-in, and a newly queued inventory refresh completing on the Mac. Two jobs are now completed. Restarted the existing foreground agent in a detached process with logs at `.protec/agent-bigbox.log`; inspect its actual PID before future restarts. Native launchd/systemd service installation is still future work.
- Verified the installed card shows Running and Settings, Secrets, lifecycle, update and backup controls. Its Open link uses private HTTPS. A normal BoxPilot backup completed as `20260908T185113Z.tar.gz`, including configuration, secrets and data. Managed data directory/database permissions are 0700/0600 with UID 10001.
- Public demo source revision and actual dashboard bytes still match the managed image. No Protec UI feature changed in this migration, so no separate public redeployment is needed.
- Found a BoxPilot Settings precheck bug: the UI sends only changed values to a precheck that resolves against catalog defaults, losing unchanged required fields/custom ports. Fixed the precheck request to include effective settings while preserving the mutation's changes-only payload; two regression cases pass. Full local check passed with 1,630 tests. BoxPilot patch v1.116.1 is tagged on main at `255270c`; hosted release/CI checks and live patch update are tracked next. Current port change succeeded using an equivalent trailing-slash HTTPS origin, normalized by Protec.

Continue M3 credential recovery/rotation and later milestones. Future releases must update `bp-protec` through the managed application workflow and verify the public mock demo against its actual image. Do not repeat the completed HTTPS or Mac cutover steps.

### Managed migration final verification

- Compared the retained original volume against managed data after retiring the original container: all prior jobs, audit events, enrollments and credentials are present. Private HTTPS readiness still returns ok; the Mac agent continues checking in. A refreshed catalog no longer lists the retired independent stack.
- BoxPilot v1.116.1 was published at `255270c8412e681c52e53e979f3dddee7d43fbb0`. Hosted CI 34265551136, install smoke 34265551145, and release 34265554378 all passed. Protec docs checkpoint `3df93b2` passed Checks 34265667153. Release: https://github.com/AES256Afro/BoxPilot/releases/tag/v1.116.1.
- The live BoxPilot still runs 1.116.0. Staged **Update to v1.116.1** in a retained browser tab and filled the version confirmation. Its owner-password requirement is the only pending step for this Settings precheck fix. The completed Protec migration, private HTTPS and Mac cutover do not depend on this patch being applied. After the owner updates, verify the live version and a Settings precheck with an unchanged required origin. Avoid creating duplicate pending update jobs.

## Recoverable credential rotation implementation, September 8

Implemented the next bounded M3 slice for 0.4.0: administrator-only service-token rotation with at most 15 minutes of overlap, unchanged role/device scope/original expiry, explicit finish and pre-deadline cancellation, one-time replacement disclosure, automatic request-time cutoff, pair revocation and transactional audit. Bootstrap and device credentials are unaffected. Schema 4 preserves earlier service credentials with nullable handover metadata. Full identity work still needs SSO/MFA and protected native storage.

Local verification: 58 Python tests and 7 JavaScript tests pass, covering lost response recovery, concurrent starts/conflicting resolutions, rollback on audit failure, exact expiry/cutoff, permission denials and scope preservation. Exported dashboard DOM exercised issue, rotate, secret clearing, cancel, retry and finish using only mock transport with network calls forbidden. This is DOM workflow validation, not a visual or native browser review.

Publication and disposable published-container validation are next. Live read-only verification at this checkpoint still found healthy `bp-protec` on 0.3.0 revision `a28698122c1ee2e1ee50157ad0524f135377911e`, and BoxPilot 1.116.0. Keep the public website/demo at the same live revision until the managed app is upgraded. Updating the managed catalog requires a new BoxPilot catalog release and its normal owner-authorized installation workflow; do not change root-managed files using Docker mounts or revive the retired independent stack. The earlier 1.116.1 owner update remains unapplied. Browser-control tools are not exposed in the current continuation context; use supported tools when available, otherwise finish source/image/catalog preparation and report the exact pending live action.

### Rotation release and rollout preparation

- Published Protec **v0.4.0**, source/image `9a8ec59f46b1df9973190e2dd6d7db1e187e71a1`, to main and tag. Hosted Checks 34286326453 and 34286329670 passed; image workflow 34286327889 passed. Multi-platform image digest: `sha256:6d797b5060614be7a56fea1b0f4d50f15aaeb877329de40f262b5cca78abd3cb`. Release: https://github.com/AES256Afro/Protec/releases/tag/v0.4.0.
- Pulled the published image on BigBox and tested a disposable Gunicorn container with no host ports or external network. Fictional scoped credentials exercised start, duplicate denial, role denial, cancellation/retry, deadline cutoff, finish, pair revocation and secret-safe listings/audit. An unfinished handover and both credentials survived container restart. Removed the temporary container and anonymous data volume after verification.
- Took a consistent read-only snapshot of the live schema-3 database to ignored `.protec/pre-rotation-live-copy.db`. Migrated another local copy `.protec/rotation-migration-check.db` to schema 4 with integrity ok and all original columns preserved: 1 device, 2 jobs, 7 audit records, 2 enrollments, 0 service credentials. These are migration test snapshots, not a substitute for a fresh rollback checkpoint at actual deployment.
- BoxPilot **v1.116.2**, source `f23cc7630163b555c6819dd67cde8653a9bfd22b`, updates the catalog to Protec 0.4.0 and corrects its HTTPS setup copy. Includes the previous Settings precheck fix. Full local check passed with 1,630 tests; hosted release 34286480408 and installation smoke 34286479164 passed. CI 34286479183 hit an existing owner-bootstrap test's five-second timeout (1,629 other tests passed); one rerun is in progress and must be checked before calling this source checkpoint verified. Release: https://github.com/AES256Afro/BoxPilot/releases/tag/v1.116.2.
- **Production is still 0.3.0/schema 3**, `bp-protec` healthy, and the public demo release/source/served dashboard hash was reverified against its actual `a286981...` revision. The 0.4.0 mock workflow is implemented and exported locally from clean `9a8ec59...` with dashboard SHA-256 `788f1c3aa27bea1585157e6be8339eb4bb8fde2e65dda5cfd33b490f1c794986`. Do not publish it ahead of the managed live upgrade.
- Remaining live action: in BoxPilot System, refresh release information and apply **v1.116.2** through the normal owner-password approval, then use **App catalog > Protec > Update available**. This supersedes the old v1.116.1 prompt; there is no need to apply both versions. BoxPilot takes a stopped-app backup checkpoint before update. After schema migration, image-only rollback is insufficient; restoring the pre-upgrade database requires the ordinary restore approval flow. Do not claim the live managed deployment is updated until its image label, schema, private HTTPS, bootstrap access, preserved records and Mac check-in are verified. Then deploy the exact `9a8ec59...` website export and use `scripts/check_release.py` against the actual live image revision.
- Found the detached Mac inventory agent no longer running during final freshness verification (last check-in about three hours old). Restarted it with the unchanged `.protec/agent.json` and log `.protec/agent-bigbox.log`, PID 20150 at this checkpoint. Verify the fresh remote check-in before claiming restored reporting. Native launchd/systemd installation is still not implemented.

Continue independent M4 typed/versioned job and receipt foundations while the owner-managed deployment step is pending. Preserve current network and credentials, and do not repeat pending approval questions each heartbeat. M3 identity federation/native storage and the full platform roadmap remain incomplete.

### Rotation checkpoint final verification

BoxPilot CI 34286479183 passed on its second attempt without source changes; hosted release and install smoke also passed. Both repositories' main/tag refs match the release commits recorded above. The published Protec manifest was inspected and contains Linux amd64 and arm64 images. Protec deployment-notes commit `50f2803` passed hosted Checks 34286853331.

The restarted Mac agent is running and remote read-only verification found a fresh check-in 22 seconds old. Production remains schema 3 on the healthy 0.3.0 image. The existing continuation automation remains active; the pending owner rollout is specifically BoxPilot **1.116.2**, followed by its managed Protec **0.4.0** update and exact-revision public demo publication. The earlier owner-bootstrap CI timeout was transient on rerun; no test timeout or product behavior was changed to bypass it.

## Owner rollout and 0.4.0 demo parity completed, September 8

The user confirmed the managed updates. Read-only SSH verification found **BoxPilot 1.116.2** and healthy **`bp-protec` 0.4.0** with source/image label `9a8ec59f46b1df9973190e2dd6d7db1e187e71a1`. The prior owner-update dependency is complete; do not ask the user to apply it again.

- Authenticated health using the existing BoxPilot-managed bootstrap token succeeded without exposing it. Live schema is 4 and integrity is ok. Counts: 1 device, 2 jobs, 7 audit records, 2 enrollments, 0 service credentials. Compared a consistent new snapshot `.protec/post-rotation-live-copy.db` with the pre-upgrade snapshot: prior device identities and all prior job/audit/enrollment/credential records are preserved. Private certificate-verified HTTPS readiness returned ok. Mac check-in was 16 seconds old.
- Exported public assets from the same clean release were validated against Git before deployment. Published the website and mock demo through the existing Cloudflare Worker, deployment `871d33d9-2b49-4419-b9ce-5a2a1fc99c74`. Public release metadata and actually served dashboard bytes match the running image, revision `9a8ec59...`, dashboard SHA-256 `788f1c3aa27bea1585157e6be8339eb4bb8fde2e65dda5cfd33b490f1c794986`.
- `/demo/` returned 200 with `connect-src 'none'`; the public `/api/dashboard` returned 404. The actually served mock transport exactly matched release source and passed rotation/cancel/retry/finish checks with network calls forbidden. Public demo data remains fictional and separate from the private fleet.

The bounded recoverable service-credential rotation slice is now deployed with matching public demo behavior. Next work is M4 typed/versioned jobs and execution receipts, retaining role/target authorization and conservative execution. Full M3 still includes identity federation and native protected storage; the broader platform roadmap is not complete. The existing continuation automation remains active.

## M4 versioned inventory delivery implementation, September 8

Implemented the first bounded M4 slice for 0.5.0: negotiated protocol-1 inventory envelopes, target validation, attempt-specific random lease tokens (hashes stored only), 120-second deadlines, three-delivery limit, transactional completion receipts bound to accepted inventory, and idempotent duplicate acknowledgements. Old agents retain read-only protocol-0 refresh compatibility; once a job is delivered as protocol 1, legacy polls/completions cannot downgrade it. The agent rejects unknown contracts and wrong targets. Schema 5 adds metadata without replacing prior job records. Dashboard and mock jobs expose attempts and receipts while labelling historical completions honestly.

Local validation: 69 Python tests and 8 JavaScript tests pass, including stale/superseded/wrong-device leases, role separation, digest mismatch, concurrent polling, duplicate acknowledgements, retry exhaustion, restart persistence, audit rollback, legacy compatibility and a real HTTP agent round trip. Exported dashboard DOM checks passed for receipt rendering and old-job labels, as well as existing credential rotation workflows. These are automated DOM checks, not a native visual review. Read `docs/JOB_PROTOCOL.md` for contract and rollout details.

Private deployment remains healthy 0.4.0 at `9a8ec59f46b1df9973190e2dd6d7db1e187e71a1`, BoxPilot 1.116.2, with matching public demo. Version 0.5.0 publication, image testing, catalog release and managed rollout are next. Preserve the existing Mac agent process until the new control plane is verified; then restart it from the updated checkout with the same `.protec/agent.json` and prove a new job has a protocol-1 receipt. Keep the public export at the live image revision until rollout completes.

This does not complete M4. Independent signatures, durable local execution receipts, capability admission for other job types, cancellation, approvals, maintenance windows and side-effect recovery remain. Do not expand the read-only compatibility protocol into a privileged execution path.

### Versioned jobs release verification and pending rollout

- Published Protec **v0.5.0**, source/image revision `52ae06e49b73a5800cb0e8aee5a89f4f08711e39`, to main and tag. Hosted Checks 34289333168 and 34289334720 passed; image workflow 34289334669 passed. Public multi-platform image digest `sha256:3f24c93c369353dd2b7c69b14b44c317168ad6302cd449d2a4044c45cc924d0a`; manifest inspection verified Linux amd64 and arm64. Release: https://github.com/AES256Afro/Protec/releases/tag/v0.5.0.
- Pulled and tested the published Gunicorn image in a disposable BigBox container with no host ports or external network. Its real agent cycle produced a protocol-1 receipt; legacy completion, no downgrade, stale/superseded attempt rejection, identical replay and secret-safe history passed. Accepted receipts and duplicate acknowledgements survived a container restart. Removed the container and anonymous data volume afterward.
- Validated a schema-5 upgrade of a consistent live schema-4 database copy, preserving every original column/record: 1 device, 2 jobs, 7 audit records, 2 enrollments, 0 service credentials. Snapshots `.protec/pre-jobs-v05.db` and `.protec/jobs-v05-migration-check.db` are ignored, private migration evidence; take a fresh managed checkpoint during actual update.
- Published **BoxPilot v1.116.3** on main/tag at `4d6572b38a62f4499fdd179344830065c53be5da`, with the Protec 0.5.0 catalog entry. Full local check passed with 1,630 tests. Hosted CI 34289453209, install smoke 34289453231 and release 34289455065 all passed. Release: https://github.com/AES256Afro/BoxPilot/releases/tag/v1.116.3.
- Final live inspection still found **BoxPilot 1.116.2** and healthy managed **Protec 0.4.0**, image `9a8ec59f46b1df9973190e2dd6d7db1e187e71a1`. Public demo source and served dashboard still match that image. **0.5.0 is released and tested, not yet deployed to the private fleet or public demo.** No live jobs were queued and no Mac agent restart was done in this implementation slice.
- Remaining action: apply the normal owner-authorized BoxPilot **1.116.3** update, then **App catalog > Protec > Update available** to deploy **0.5.0**. This is a new release, not a repeat of the completed 0.4.0 rollout. Browser controls remain unavailable in this continuation context, and owner authentication must stay in BoxPilot's normal flow. Do not bypass it through root mounts or authentication-state edits. Avoid repeating the request every heartbeat.
- After the user confirms the update, inspect actual versions and schema, verify private HTTPS and preserved records, then restart the existing Mac agent with the unchanged state file. Verify a newly queued refresh produces a protocol-1 receipt before claiming native receipt support is active. The public 0.5.0 export is ready at `website/dist`, built from clean `52ae06e...`, dashboard SHA-256 `750797e435da5270ed5ff14f587704a521f58f0f8116235041b21d21b77c8bbc`; validate it against the actual image before Cloudflare deployment. If rebuilding from a newer docs-only HEAD, use a clean worktree at the image revision so release.json reports the image's exact source.

Continue independent M4 cancellation/signature/local-receipt foundations while the managed rollout is pending. The completed source/image checkpoint does not complete M4, native deployment validation, or the infrastructure platform roadmap. Keep the continuation active.

## Unreleased M4 cancellation source checkpoint, September 8

The existing 0.5.0 rollout was still pending at this heartbeat: live BoxPilot 1.116.2 and healthy Protec 0.4.0 (`9a8ec59...`). No duplicate owner-update request or new catalog release was created. The public demo remains matched to the current private image.

Implemented inventory-job cancellation on main after the 0.5.0 tag. Operators/administrators can cancel queued or running refreshes using the job's stored device scope; a spoofed body device cannot grant access. Cancellation invalidates the lease, prevents redelivery and late completion, and records one transactional audit event. Duplicate cancellation is idempotent. Completion/cancellation races have one winner and accepted receipts remain intact. An already running read may still finish on the device; ordinary check-ins continue.

The dashboard and job history expose Cancel refresh where permitted. Mock source includes queued and running examples, accurate pending counters, duplicate-queue checks and cancellation behavior. Local validation passed with **76 Python tests and 9 JavaScript tests**, plus dashboard/history DOM checks for confirmation decline, cancellation and counters without network access. Tests cover both protocols, wrong-target scope spoofing, viewer/device denials, audit rollback, concurrent completion and restart persistence. No schema change was added beyond schema 5.

This is an **unreleased source checkpoint**, not a change to the 0.5.0 tag/image, the catalog or either deployed website. Keep VERSION/catalog release references as published until the next release is deliberately prepared. Do not deploy an export of the newer working tree while the managed image is older. For the pending 0.5.0 rollout, use the exact `52ae06e49b73a5800cb0e8aee5a89f4f08711e39` source in a clean temporary worktree to build the public export, and use the agent from that release for the native round-trip check. Then verify the actual container label, schema, preserved data, bootstrap access, fresh Mac check-in and a protocol-1 receipt before publishing its matching mock assets.

Next independent M4 work: durable local receipts and signature design/verification, followed by capability admission, approval and maintenance-window foundations. Do not enable privileged operations before their isolated execution/recovery gates. The broader platform roadmap remains incomplete and continuation stays active.

## Unreleased M4 local receipt source checkpoint, September 8

Added an owner-only SQLite journal beside the command-line agent state. The journal binds to one server/device, commits attempt and inventory-digest evidence before completion submission, suppresses repeated processing of the same recorded attempt, and validates server acknowledgements. A narrow device-authenticated receipt lookup reconciles lost responses without resubmitting completion or retaining lease tokens. Cancelled, failed, superseded and unknown work remain distinct. The offline `--receipt-status` command exposes metadata only. Capacity preserves unresolved evidence and prunes only old terminal records. See AGENT_RECEIPTS.md.

Local validation passed with **88 Python tests and 9 JavaScript tests**, plus dashboard syntax and website export checks, including abrupt process exit after a pending commit, real HTTP lost-response recovery, wrong-device lookup, conflicting receipts, unsafe file permissions, journal binding, capacity and offline status. Real power-loss testing and native service reboot/update behavior remain unverified. This remains read-only inventory infrastructure, not authorization for privileged operations.

A fresh inspection after the user's confirmation still found healthy Protec **0.4.0** at `9a8ec59f46b1df9973190e2dd6d7db1e187e71a1` and BoxPilot **1.116.2**. The released 0.5.0 rollout remains pending. This source checkpoint does not bump VERSION, publish an image/catalog release, restart the Mac agent or deploy either site. For eventual 0.5.0 verification and public parity use the exact release source described above, since main now contains unreleased cancellation and receipt recovery. Next independent M4 work is signature verification and capability admission.

The public release check confirmed the served demo source and dashboard bytes still match the live 0.4.0 image. The local website build is a verification artifact from newer main and must not be published for the pending 0.5.0 rollout.

## Unreleased M4 capability admission source checkpoint, September 8

Completed the next independent read-only execution foundation while the 0.5.0 rollout remains pending: a bounded per-heartbeat capability report, server admission against explicit supported contracts and agent-side checks against its own advertised handlers. Empty or incompatible reports withhold a new inventory lease without consuming attempts. Malformed reports reject atomically before updating inventory or last-seen time. Active leases can still complete after capability withdrawal; existing scoped cancellation remains the way to cancel them. Omitted reports preserve only the existing inventory compatibility path, which cannot silently expand when future handlers are registered.

Capability reports are self-reported compatibility information, not attestation, privileges or operator authorization. They are evaluated on every poll and are not persisted as inventory. No schema migration, dashboard feature, new executable kind or public mock behavior was introduced. See JOB_CAPABILITIES.md for the wire contract and limits. Independent job signatures, trust provisioning and admission/recovery for privileged kinds remain open.

Local validation: **101 Python tests and 9 JavaScript tests passed**, plus dashboard syntax, website export and diff checks. The 13 added tests cover atomic malformed-report rejection, empty/unknown/mismatched reports, attempt preservation, restart and concurrent polls, active completion after withdrawal, exhaustion, fixed legacy fallback, agent refusal before journal/forced collection, role separation and a real HTTP cycle with two advertised polls and a matching protected local receipt. The HTTP test uses disposable package fixtures; no package installation or network changes were performed. Existing local-receipt source commit `9cd44d695089679a7d251da03bc023852f87309a` passed hosted CI 34293136573 on Python 3.11 and 3.14 before this slice.

Fresh live inspection still found healthy Protec **0.4.0** and BoxPilot **1.116.2**. Keep the public export matched to that image. This is another unreleased main checkpoint, with VERSION and published image/catalog references unchanged. No live jobs, Mac agent restart or managed update was performed. Do not publish the current main website build over the live 0.4.0 demo; use exact release source when the managed rollout happens. Next work is job signature/trust verification, followed by approval and maintenance-window foundations. The broader roadmap remains active.

## Unreleased M4 pinned inventory signatures, September 8

Implemented optional Ed25519 signatures for protocol-1 inventory jobs. The server signs the complete envelope and exact portal origin inside the inventory/lease transaction; signing failure rolls back the transaction. Operators provision public trust files independently. Agents started with `--job-trust` refuse missing, altered, expired, wrong-device, wrong-origin or unknown-key deliveries before local attempt recording or forced collection. Existing envelope, capability, lease, cancellation and journal checks still apply. Completion receipts themselves remain unsigned. Read JOB_SIGNATURES.md for the canonical byte contract and key lifecycle.

Added an exclusive key-generation CLI, owner-only bounded key/trust file loading, server `--job-signing-key`, container `PROTEC_JOB_SIGNING_KEY` configuration and explicit agent trust loading. Public trust can contain up to eight keys for operator-managed overlap. Keys load at startup; removal/rotation requires restarts. No remote trust learning, automatic rotation, BoxPilot key provisioning or native protected key store is claimed. Legacy unsigned read-only agents remain compatible. Signed mode must become a mandatory admission requirement before privileged job types are introduced.

The optional signing dependency is pinned in requirements-signing.txt and included in the source Docker build. Local checks used an isolated project environment at `.protec/signature-test-venv`, without modifying system Python or installed OS packages. The full suite passed with **116 Python tests and 9 JavaScript tests**, plus dashboard syntax, website export and diff checks. New tests include field-by-field tampering, origin/key binding, stale and superseded leases, missing/downgraded proofs, strict encoding and protected-file checks, manual trust overlap/removal, transaction rollback, real HTTP/WSGI delivery and actual subprocess key-generation/agent CLI execution. The CLI test verifies a pinned agent refuses the next job when server signing is removed.

Built the unreleased source image on BigBox and exercised it in a disposable container with no external network, host ports or host mounts. The actual Gunicorn entrypoint loaded the configured private key, delivered a signed job, and the pinned agent verified it and recorded an acknowledged local receipt. Removed the disposable container/anonymous volume, temporary image tag and uploaded source directory afterward. This was a source-image smoke test, not a published release or live fleet update.

Fresh verification still found healthy managed Protec **0.4.0** and BoxPilot **1.116.2**. The public release checker confirmed the served demo source and dashboard bytes match the running 0.4.0 revision. No live jobs were queued, no existing agent or credentials were replaced, and the pending 0.5.0 rollout remains separate. VERSION/image/catalog references are unchanged. The prior capability checkpoint `4bba31dfa52e3710501451cab2bd16a085fd8b82` passed hosted CI 34294382356 on Python 3.11 and 3.14. Publish this source checkpoint to main and verify its own hosted CI before reporting completion.

Next independent M4 work is approval and maintenance-window foundations, while live signature provisioning, catalog setup, key recovery, native service reboot/update and privileged execution/recovery remain open acceptance gates. Keep the roadmap continuation active and build the public export from the actual deployed release, not newer unreleased main.

### Signature checkpoint publication and build-context check

Source commit `56c39fea8b9ded8f3cbc63dcd652753263c84f36` was pushed to main and passed hosted Checks 34295923726 on Python 3.11 and 3.14. A follow-up Docker exclusion check placed disposable, non-secret files named signing-key.json and job-trust.json inside the otherwise included protec source directory. The built container contained neither file. Explicit exclusions now protect both generated filenames throughout the Docker context as well as Git. Removed this second disposable container/image and temporary context after verification.

## September 9 four-hour continuation and inventory maintenance windows

The user requested another four hours of work. Updated the existing `protec-four-hour-platform-build` heartbeat, keeping its cadence and task target, to work through **2026-09-09T14:32:28Z (9:32 a.m. Central)**. This replaces the earlier unlimited scheduled run. At the deadline, finish current verification safely, report completed/remaining work and pause the automation. Do not mark the whole platform complete because the time window ends.

Implemented one-shot inventory maintenance windows in unreleased schema 6. `POST /api/jobs` accepts bounded integer UTC start/end values. Early polls consume no attempt, delivery leases are clipped to the window end, and expired pending work becomes failed with one transactional audit event on the next device heartbeat. Capability, scope, cancellation, legacy protocol and optional signature rules remain effective. Existing records migrate with null bounds and retain immediate behavior. Creation is API-only in this slice; the dashboard and history show the scheduled window, and browser-only mock source simulates waiting, an in-window check-in and expiry. See JOB_WINDOWS.md.

Validation passed with **129 Python tests and 10 JavaScript tests**, plus dashboard syntax, website export and diff checks. Added boundary, signed-lease, legacy, restart/concurrency, cancellation/revocation, capability, scoped HTTP, invalid-input, audit rollback and schema-5/6 migration tests. Public demo verification still matches the deployed 0.4.0 revision; the new main export must not be published ahead of the managed image.

Tailscale SSH initially required a fresh authentication check. The user completed it and SSH verification resumed, finding healthy Protec **0.4.0** at `9a8ec59f46b1df9973190e2dd6d7db1e187e71a1` and BoxPilot **1.116.2**. No further Tailscale authentication request is pending from this turn. Took a consistent read-only live snapshot to `.protec/maintenance-20260909-1032-before.db`, then migrated its separate copy `.protec/maintenance-20260909-1032-migrated.db` from schema 4 to 6. All original columns/records were preserved: 1 device, 2 jobs, 7 audit entries, 2 enrollments and 0 service credentials. Integrity is ok; original jobs have null window bounds. Production data and the existing agent were not changed.

The pending released **0.5.0** rollout remains distinct and must still use exact release source `52ae06e49b73a5800cb0e8aee5a89f4f08711e39`, not current schema-6 main. No VERSION, image or catalog release was changed. Previous signing source and container-exclusion checkpoints passed hosted CI 34295923726 and 34296012700. Publish this maintenance-window checkpoint to main and verify its exact hosted CI. Next bounded work: scheduling UI and review/approval foundations, followed by a deliberately prepared release and its managed/native/public parity gates within the authorized work window.

## Unreleased scheduling interface checkpoint, September 9

Completed the inventory scheduling UI within the active four-hour window ending 14:32:28 UTC. Devices now offer Schedule refresh to permitted operators when the server explicitly advertises `job_windows: 1`. Older servers, viewers, revoked devices and already-pending refreshes do not expose a usable scheduling action. The dialog identifies the device, shows local start/end inputs and a UTC preview, validates skipped/repeated DST times and bounds, and submits the existing schema-6 window contract. Cancellation clears the draft. In-flight requests block duplicate submits/dismissal; a successful queue followed by dashboard reload failure is reported as scheduled instead of inviting resubmission.

Real and mock dashboard responses advertise window support. The same exported dashboard uses the browser-only mock transport for scheduling and Activity/history display. Removed the stale hard-coded Version 0.3 sidebar label in source. No database migration beyond schema 6 was introduced and no release/image/catalog version was bumped.

Validation passed with **129 Python tests and 17 JavaScript tests**, plus dashboard syntax, public export and diff checks. Seven new UI workflow tests cover local-to-UTC conversion, old-server/viewer/revoked/pending guards, invalid/expired windows, permission loss during a draft, spring-forward gaps and fall-back first-occurrence preview, duplicate submissions, draft cancellation, lost responses and reload failure after a successful queue.

An isolated headless Chrome profile loaded a temporary local mock export. Native form submission created a queued mock job, closed the dialog and updated counters. Inspected desktop and genuine 390-pixel emulated viewport screenshots; the dialog stayed inside the viewport with no internal horizontal overflow. Direct Chrome window-size flags produced a mismatched narrow capture, so the final narrow verification used an explicit DevTools viewport override and measured the dialog bounds (358 pixels wide within a 390-pixel viewport). Evidence is in ignored `.protec/schedule-ui-desktop.png`, `.protec/schedule-ui-mobile-cdp.png` and `.protec/schedule-ui-submitted.png`. Closed the isolated browser and local preview server. No existing browser profile, fleet endpoint or agent state was used. Native picker keyboard interaction, Safari/Firefox and real mobile devices remain unverified.

Fresh BigBox inspection still found healthy Protec **0.4.0** and BoxPilot **1.116.2**. The public release check confirms the served demo matches the actual 0.4.0 image. The pending 0.5.0 managed rollout is unchanged; do not publish schema-6 main assets ahead of the deployed image. The previous maintenance-window commit `d09ee5d213add254170562f8fa42c7546b172a90` passed hosted CI 34341566604. Publish this UI checkpoint and verify its exact hosted CI. Next independent work is review/approval foundations and a deliberate release checkpoint, with native/managed/public parity gates kept separate. The four-hour continuation remains active.


## Five-hour window and revised delivery order, September 9

The user reviewed the plan, then requested another five hours. Updated and resumed the existing continuation (it was paused when inspected), keeping its thread and ten-minute cadence, with a new deadline of **2026-09-09T16:24:56Z (11:24:56 a.m. Central)**, five hours after 11:24:56 UTC. This supersedes the previous 14:32:28 UTC deadline. At the new deadline, close verification safely, report actual progress and pause the automation.

The revised priority is a usable Linux management pilot: consolidate the tested execution foundations into a deliberate release, verify independent Compose and managed BoxPilot installation plus matching demo, establish a persistent Linux agent on a disposable target, then add narrow static groups/policies and a complete APT plan/approve/execute/verify workflow. Defer a generic approval queue for read-only inventory. Plan approvals must bind exact operation/device/parameters, and separate tokens do not establish separate human approvers. The long-term platform document now explicitly distinguishes proposals from accepted architecture/licensing/program decisions.

The plan review reverified healthy BigBox Protec 0.4.0 at `9a8ec59f46b1df9973190e2dd6d7db1e187e71a1`, BoxPilot 1.116.2, and exact public demo parity. Source scheduling checkpoint `84130fbad2aafb86a64c99a7bf1fa497f6c782ea` has passed hosted Checks 34343561936 on Python 3.11 and 3.14. The interrupted approval investigation made no source edits and started no implementation. The pending 0.5.0 owner rollout remains separate; inspect it before choosing a subsequent release. No live service, agent, credential or public assets were changed in this planning/scheduling update.


## Independent Compose release acceptance, September 9

Within the five-hour window ending 16:24:56 UTC, inspected the pending rollout again: healthy managed Protec 0.4.0 at `9a8ec59f46b1df9973190e2dd6d7db1e187e71a1`, BoxPilot 1.116.2. No managed update has happened. Public source/dashboard parity still passes for that running image.

Added `scripts/check_compose.sh` and its internal `container_smoke.py` helper as a repeatable release gate, now run by hosted Checks. The wrapper creates a unique project and fresh volume, removes published ports, disables the container's external network and keeps the supplied Compose hardening. It verifies the actual configuration before running checks through the real Gunicorn HTTP interface. The helper refuses an already populated fleet and prints no tokens or device records. It tests independent file-token bootstrap, single-use enrollment, viewer/device denials, cross-device operator scope, scoped dashboard isolation, protocol-1 receipts/replay, early-window withholding, cancellation idempotency, online backup/staged restore, UID and data permissions. Container restart must preserve credentials, fleet/jobs/audit, scheduled work and receipt replay.

Built the unreleased source image on BigBox and passed the final wrapper against it in a disposable Compose project. The test had no host mounts, host port bindings or external container network. Verified removal of the final project's containers and volumes, removed the temporary image/source directory, and retained secret-free evidence in ignored `.protec/compose-acceptance-20260909.log`. This is independent Compose and container restart evidence, not a managed rollout, OS reboot test or native agent-service verification. Local checks passed: 129 Python tests, 17 JavaScript tests, dashboard syntax, website export, shell syntax and diff checks.

Restricted image publication to version tags matching VERSION, removing manual main-branch dispatch that could overwrite a published version tag with newer source. Independent install documentation now checks out a release tag explicitly, explains main/source-build version risks and documents the repeatable Compose gate. VERSION and all published image/catalog references remain unchanged. No real agent, production volume or public demo assets were changed. The previous planning commit `f8a9e01286f2bfba5991bd4030e27d4065b6e05c` passed hosted Checks 34345557067. Publish this acceptance checkpoint and verify its own hosted CI, including the new Compose job.

Next: deliberately prepare the consolidated 0.6 release from the tested foundations, including honest release-version reporting and updated feature/upgrade documentation; validate and publish its exact image before changing the BoxPilot catalog. The currently pending 0.5 release remains immutable. If managed owner authentication remains pending, continue the Linux agent installation slice independently and keep the public export matched to the actual running image.


## Continue until completion, September 9

The user instructed "Continue working until completed." Updated the existing heartbeat to **Protec platform build**, ACTIVE with its existing ten-minute cadence and thread, removing the five-hour cutoff and all deadline-driven stopping instructions. This supersedes the 16:24:56 UTC deadline recorded above. Continue bounded verified slices until roadmap acceptance is satisfied, the user stops work, or external dependencies prevent all meaningful independent progress. Keep pending owner rollout separate and continue independent implementation while it waits.

The independent Compose acceptance checkpoint `131a202a8e6ef8a0b84ac7e4ceaa8475c651ef6a` passed hosted Checks **34346937684**, including Python 3.11, Python 3.14 and the new real Compose installation/backup/restore/restart job. Next work remains the consolidated release, followed by the persistent Linux agent and narrow policy/package workflow. No release or deployment changed in this scheduling update.

## Consolidated 0.6 release preparation, September 9

Prepared VERSION/independent Compose 0.6.0, consolidating cancellation, protected local receipts, capability admission, optional pinned inventory signatures and schema-6 maintenance windows/scheduling. Updated feature and upgrade documentation and added RELEASE_0_6.md. Agent inventory and authenticated control-plane health now read the packaged VERSION file; the health view and mock report the release honestly. Docker explicitly includes VERSION. This fixes the agent's old hard-coded 0.1.0 report without changing enrollment identity.

Local checks passed: **131 Python tests and 17 JavaScript tests**, dashboard syntax, website export and diff checks. Took a fresh consistent live schema-4 copy to ignored `.protec/v06-release-before.db` and upgraded its separate `.protec/v06-release-migrated.db` copy to schema 6. Every original column/row was preserved: 1 device, 2 jobs, 7 audit records, 2 enrollments, 0 service credentials; integrity passed and historical window bounds remain null. A disposable container using the actual published 0.5 image correctly refused the upgraded schema-6 copy. Rollback requires the older snapshot/image, not merely changing the image tag back.

Live inspection still found healthy Protec 0.4.0 and BoxPilot 1.116.2. No production migration, Mac agent restart, live job or public-site change occurred. The old 0.5 release remains immutable. Commit/push this prepared release, verify exact hosted Checks including Compose, then tag/publish 0.6 and verify its image on both architectures and through the disposable Compose acceptance wrapper before updating the BoxPilot catalog. Track subsequent publication/deployment evidence below. Previous continuation-doc commit `0dd46b244e86ac71fd892790a27bdcf83d50c323` passed hosted Checks 34347339238.


### Protec 0.6 publication and catalog release checkpoint

Published Protec **v0.6.0**, source/image revision **`c9e0117e5a1bcd0e1b853c9e0955a6c1979798ad`**, with main/tag matching at publication. Exact hosted Checks **34348537258** and tag Checks **34348678025** passed, including real Compose acceptance; image publication **34348677941** passed after its own required Compose gate. Release: https://github.com/AES256Afro/Protec/releases/tag/v0.6.0. Image index digest **`sha256:8436811789b84a4562d70f2f16bba2ec7ca96e487530aee42f320b62090b2cd6`**. Manifest inspection verified Linux amd64 and arm64; BigBox lacks the buildx subcommand, so used `docker manifest inspect` instead.

The actual published image passed the independent Compose wrapper on BigBox with no published ports, external container network or host directory mounts. Verified its source label, then removal of the disposable container/volume and temporary source directory. Retained secret-free log `.protec/v06-published-image-acceptance.log`. A separate run of the actual previous 0.5 image also opened a restored pre-upgrade snapshot and preserved its counts/integrity, while the same old image refused the schema-6 copy. These are disposable release/recovery checks, not a live managed update.

Prepared and pushed BoxPilot **v1.116.4**, commit **`5c1f347`**, with the Protec 0.6 catalog image/version, scheduled-refresh/cancellation description and schema-6 rollback note. Local `npm run check` passed with **1,630 tests**. Hosted CI **34349125427** and install smoke **34349125407** passed before pushing the matching version tag. The tag triggers the normal hosted release workflow; verify its completion/release before reporting that BoxPilot release as published. No special app-specific deployer logic or owner-authentication bypass was added.

Fresh private/public checks still found healthy **Protec 0.4.0 / BoxPilot 1.116.2**, with the demo source and served dashboard matching `9a8ec59f46b1df9973190e2dd6d7db1e187e71a1`. The new deployment target is **BoxPilot 1.116.4, then managed Protec 0.6.0**, superseding the pending 1.116.3/0.5 target; do not require an intermediate 0.5 deployment. Older releases/tags remain unchanged. Apply updates through the normal owner-authenticated flow, without repeating that request on each heartbeat. Publishing a new catalog does not update the running services.

After owner rollout, verify actual image revision/schema 6, bootstrap/private HTTPS, preserved records and agent freshness. Restart the Mac foreground agent from the exact 0.6 release with its unchanged enrollment state and verify a fresh protocol-1 receipt; manually configured signing/trust remains a separate setup gate. Build the public export from clean `c9e0117e5a1bcd0e1b853c9e0955a6c1979798ad`, then publish only after the private image is verified. Its dashboard hash is **`0769b270aecc613b0e241b6da919e2dcf1328b51083939e7360555be901843cd`**; the clean export currently exists in ignored website/dist. Do not publish a later main build accidentally.

Next independent milestone: persistent Linux agent installation, service restart/update/uninstall and actual guest reboot tests. Read-only host discovery found virsh and qemu-system-x86_64 installed on BigBox, with an empty per-user libvirt session; the SSH account had neither read nor write access to /dev/kvm. Investigate a disposable user-mode software-emulated guest or an authorized BoxPilot VM path; do not change host KVM permissions or bypass owner controls merely for testing. Container restart evidence does not substitute for a Linux guest reboot. The continuation remains active without a deadline, and the broader roadmap is incomplete.


## New five-hour window, September 9

The latest user request starts a new five-hour window at **2026-09-09T12:22:49Z**, ending **2026-09-09T17:22:49Z (12:22:49 p.m. Central)**. Updated the existing heartbeat to ACTIVE with its existing cadence/thread and this deadline, replacing the prior unlimited instruction. At the deadline, conclude current verification safely, report completed/remaining work and actual deployment, then pause. Next independent work remains the Linux service installation and disposable guest, then narrow policies/packages; the managed target remains BoxPilot 1.116.4 and Protec 0.6.0.

Final prior-turn verification confirmed BoxPilot release workflow **34349453075** succeeded and **v1.116.4** is published (not draft/prerelease), with main/tag at `5c1f34787a9f15fe0539e8590bb7162c59c0c61e`. Protec deployment-notes commit `c541114` passed hosted Checks **34349524447**. Both 0.6 image publication and catalog publication are complete; managed rollout and public demo update remain separate. No new product code or deployment changed in this time-window update.


## Linux inventory service and native guest acceptance, September 9

Implemented the first persistent Linux inventory installation slice after the published 0.6 tag. `scripts/linux_agent.sh` installs from a trusted local checkout into immutable root-owned version/fingerprint directories, selects code with an atomic link, creates a dedicated non-login account and a hardened systemd unit, and enrolls via a hidden prompt. The daemon is unprivileged with no effective capabilities and NoNewPrivileges. State/receipt directories stay protected and survive upgrades. A failed service activation restores the prior code/unit when available. Uninstall stops/disables the service and removes code/unit while retaining enrollment, receipts and the account for recovery. See LINUX_AGENT.md for exact usage and limitations. No package manager or Python dependency installation is performed by the helper.

Created a genuinely separate Ubuntu 24.04.4 guest on BigBox using existing unprivileged QEMU software emulation, 2 virtual CPUs, 1536 MiB memory and an isolated 8 GiB thin disk. Canonical image SHA-256 verified against the same HTTPS source: `d0fe84bb5f80853425fa6be28e2c106f30104c3cfe8611933f2e65c9b63f0e30`. No KVM permission changes or host systemd units were needed. The guest network uses QEMU restriction with only a host-loopback SSH forward, plus a NoCloud seed with a dedicated lab key. There is no real fleet endpoint or host directory mount in the guest.

The native `linux_agent_smoke.py` harness passed installation/enrollment, actual dpkg inventory, state/source permissions, non-root UID/capabilities, scoped inventory receipts, symlinked-source refusal, duplicate-enrollment refusal, intentional failed-activation rollback, synthetic version upgrade and repeated install. Rebooted the actual guest kernel, verified a different boot ID and a fresh automatic check-in after boot, then verified a new receipt, uninstall, reinstall with the same enrolled identity and final uninstall. Test versions 0.6.98/0.6.99 existed only in the disposable guest. The first harness assertion incorrectly expected package status `ok`; corrected it to the existing real contract `complete`, reset only the marked guest fixture and reran successfully. The public mock source had the same old status spelling; corrected that fixture and added an assertion. No management feature is inferred from mock status.

Local validation passed **131 Python tests and 17 JavaScript tests**, dashboard syntax, website export, Python/shell syntax and diff checks. Hosted Checks now includes shell syntax for installation scripts; native systemd/reboot verification is the separately recorded guest run, not a claim about the CI container. Package-signature provisioning for this service, signed installer/update artifacts, full power-loss installer recovery, later health-based rollback, OS credential stores, native macOS/Windows installation and additional Linux distributions remain open gates. This helper currently uses unsigned read-only inventory compatibility, and does not grant administrator execution rights.

The lab lives at `/home/bigbox/protec-linux-lab` on BigBox; guest disk/key/seed are retained privately for subsequent isolated policy/package tests and the guest is being powered off after acceptance. Local ignored `.protec/linux-lab` contains its seed/key/source bundle. Restart only this named lab guest when needed; do not install the agent onto BigBox or this Mac just to test it. Current source and guest SSH instructions can be reconstructed from this turn; keep private key contents out of logs/source. The guest-only fixture control plane is under /opt/protec-lab with its data under /var/lib/protec-lab. Test account lab has sudo only inside that disposable guest.

Live deployment was rechecked at the start: healthy Protec **0.4.0** / BoxPilot **1.116.2**, with the **0.6.0 / 1.116.4** managed target still pending. This is an unreleased source/agent checkpoint: no version/tag/catalog bump, real agent restart, production job or public export publication. After commit/push, verify exact hosted CI. Next independent slice is narrow groups and versioned policy evaluation using inventory/package evidence, then APT preview/approval/execution in the isolated guest. The active window still ends **2026-09-09T17:22:49Z**; check time before each slice and pause at that deadline after a safe checkpoint.
